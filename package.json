{
  "name": "opencode-voice",
  "version": "0.1.0",
  "description": "Voice input for opencode: /r to record, /s to stop — whisper transcript lands in the TUI prompt input",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "bun-types": "^1.1.0",
    "typescript": "^5.5.0"
  },
  "license": "MIT"
}
